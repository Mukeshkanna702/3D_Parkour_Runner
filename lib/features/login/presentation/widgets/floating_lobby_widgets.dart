import 'package:flutter/material.dart';

class FloatingLobbyWidgets extends StatelessWidget {
  const FloatingLobbyWidgets({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
